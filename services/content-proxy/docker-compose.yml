services:
  content-proxy:
    build: .
    ports:
      - "3100:3100"
    environment:
      - PORT=3100
      - PROXY_SECRET=${PROXY_SECRET}
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
